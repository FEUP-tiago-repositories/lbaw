<nav
    class="sticky top-[100vh] h-22 text-lg text-emerald-800 mx-auto flex items-center px-20 py-4 justify-end bg-white b font-medium border underline transition-colors duration-300 ease-in-out">
    <div class="flex items-center">
        <a href="{{ route('home') }}"
            class="hover:text-emerald-400 mr-4">Terms
            Of Service</a>
        <a href="{{ route('home') }}"
            class="hover:text-emerald-400 mr-4">FAQ</a>
        <a href="{{ route('home') }}"
            class="hover:text-emerald-400 mr-4">Contact
            Us</a>
        <a href="{{ route('home') }}"
            class="hover:text-emerald-400">About
            Us</a>
    </div>
</nav>